import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  card: {
    padding: 20,
    borderRadius: 20,
    marginBottom: 15,
    width: '100%',
  },
  title: { color: '#FFF', fontSize: 18, fontWeight: '600' },
  count: { color: '#FFF', opacity: 0.8, marginTop: 4 },
  progressWrapper: { marginTop: 15, width: '100%' }
})