import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  body: {
    fontSize: 16,
    color: '#FFFFFF',
  },

  h1: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#FFFFFF',
    marginBottom: 10,
    textAlign: 'center',
  },

  label: {
    fontSize: 14,
    color: '#CCCCCC',
    marginBottom: 4,
  },

  link: {
    fontSize: 14,
    color: '#78C1EE',
    marginBottom: 4,
  },

  description: {
    fontSize: 14,
    color: '#FFFFFF',
  }
});